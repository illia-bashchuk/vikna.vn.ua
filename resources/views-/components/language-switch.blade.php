<div class="language-switch">
    <nav class="nav justify-content-center">
    <a class="nav-link {{ app()->getLocale() === 'uk' ? 'my-active' : ''}}" href="{{$ukr_url}}">укр</a>
    <a class="nav-link {{ app()->getLocale() === 'ru' ? 'my-active' : ''}}" href="{{$rus_url}}">рус</a>
    </nav>
</div>

